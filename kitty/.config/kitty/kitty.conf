clear_all_shortcuts yes
clipboard_control write-clipboard write-primary read-clipboard-ask read-primary-ask

font_family JetBrainsMono Nerd Font Mono
font_size 16.0
disable_ligatures always
cursor_shape block
cursor_blink_interval 0
cursor_trail 1
cursor_trail_start_threshold 10
cursor_trail_decay 0.1 0.3 
enable_audio_bell no

map cmd+v paste_from_clipboard

# tmux (prefix C-s): window index stays cmd+1–5; other tmux actions use cmd+shift; paste = cmd+v
# window prev/next
map cmd+shift+] send_text all \x13]
map cmd+shift+[ send_text all \x13[
# jump to window 1–5
map cmd+1 send_text all \x131
map cmd+2 send_text all \x132
map cmd+3 send_text all \x133
map cmd+4 send_text all \x134
map cmd+5 send_text all \x135
# new / close window / close pane
map cmd+shift+t send_text all \x13n
map cmd+shift+w send_text all \x13q
map cmd+shift+x send_text all \x13x
# splits: s = pane below, v = pane right
map cmd+shift+s send_text all \x13s
map cmd+shift+v send_text all \x13v
# Shift+Enter sends CSI 13;2u (distinct from regular Enter)
map shift+enter send_text all \x1b[13;2u
# Cmd+[/] sends C-o/C-i for vim jump back/forward
map cmd+[ send_text all \x0f
map cmd+] send_text all \x09

include catppuccin-mocha.conf
